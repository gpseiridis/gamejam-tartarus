using UnityEngine;
using System.Collections;

public class CameraController : MonoBehaviour {

	public GameObject player;
	public float rotspeed;

	// Update is called once per frame
	//otan kouname thn kamera paei sto lateUpdate
	void LateUpdate () {
		//theloume h kamera na nai ston pextei

		//transform position gia na paroume ta X Y Z
		//transform.position = player.transform.position;

		//Quaternion rot = player.transform.rotation;
		//transform.rotation = Quaternion.Slerp (transform.rotation, rot, Time.deltaTime * rotspeed);



 	



	
	}
}
